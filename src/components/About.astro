---
import { getCv } from '../i18n/index.js';
import Section from './Section.astro';

const cv = getCv(Astro.currentLocale ?? 'es');
---

<Section id="sobre-mi" nav={cv.sections.about.nav} title={cv.sections.about.title}>
	<div class="prose">
		{cv.about.map((p) => <p>{p}</p>)}
	</div>
</Section>
